Installation
============

Install the core package from PyPI:

.. code-block:: console

   pip install privateassets

The optional factor-loading estimator is available separately:

.. code-block:: console

   pip install "privateassets[factors]"
